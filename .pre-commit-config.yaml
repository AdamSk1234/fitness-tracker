repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.5.7
    hooks:
      - id: ruff
        args: [--fix, --unsafe-fixes]
        exclude: '(^|/)migrations/'
      - id: ruff-format
        exclude: '(^|/)migrations/'
